{
  "name": "kenaz-website-monitor",
  "version": "1.0.0",
  "description": "Hourly automated flow monitor for kenazperfumes.com",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
